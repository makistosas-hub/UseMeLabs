# Local environment variables
# Copy to .env.local and fill in your values.

# Option A (recommended): paste the full Chatlio embed snippet
# NEXT_PUBLIC_CHATLIO_EMBED_CODE="<!-- paste Chatlio script here -->"

# Option B: use widget ID only (Chatlio script will be loaded automatically)
# NEXT_PUBLIC_CHATLIO_WIDGET_ID="your-widget-id"

# Optional: override the base URL used in metadata/sitemap (defaults to http://localhost:3000)
# NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional: send analytics events to your own endpoint (disabled by default)
# NEXT_PUBLIC_ANALYTICS_ENDPOINT="https://your-endpoint.example/events"
